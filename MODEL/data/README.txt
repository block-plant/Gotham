# DATA_PATH_CONFIG
# The large data files remain at e:\GNN\data\ to avoid duplication.
# MODEL scripts will look for data at ../data/ relative to MODEL/
# graph_tensors is junction-linked at MODEL/data/graph_tensors -> data/graph_tensors
